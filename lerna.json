{
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": [
        "ignored-file",
        "*.md"
      ],
      "message": "chore(release): publish",
      "registry": "https//localhost:4873"
    }
  },
  "version": "independent",
  "npmClient": "yarn"
}
